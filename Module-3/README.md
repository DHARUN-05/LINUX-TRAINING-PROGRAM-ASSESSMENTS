This module contains the bash script for backing up files from the source directory to backup directory by using Quoting, Globbing, Array operations, Conditional executions.
And also the outputs are attached as ".txt" files.
